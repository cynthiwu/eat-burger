<div>
<img id="burger-img" src= "img/burger.png" alt="Image of a burger">
</div>

<h1 class="page-head">Eat-Da-Burger!</h1>

<h2>Burgers to Eat</h2>

<ul>
  {{#each burgers}}
    {{#unless devoured}}
      
    {{/unless}}
  {{/each}}
</ul>

<h2>Burgers Eaten</h2>

<ul>
  {{#each burgers}}
    {{#if devoured}}
      
    {{/if}}
  {{/each}}
</ul>

<h2>Add a Burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Enter a burger you want to eat and click the "Submit" button.</label>
    <input type="text" id="ca" name="burger">
  </div>

  <button type="submit">Submit</button>
</form>
